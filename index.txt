<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>拼图大师 - v4.7 修复版</title>
    <script src="https://unpkg.com/@supabase/supabase-js@2"></script>
    <style>
        :root {
            --primary-color: #4a90e2;
            --bg-dark: #0f0f1e;
            --card-bg: rgba(255, 255, 255, 0.05);
            --gold: #f1c40f;
            --success-green: #2ecc71;
        }

        body {
            font-family: 'PingFang SC', sans-serif;
            background-color: var(--bg-dark); color: white; margin: 0; 
            min-height: 100vh; display: flex; flex-direction: column; align-items: center; 
            overflow-x: hidden; touch-action: manipulation;
        }

        #user-bar {
            width: 100%; max-width: 800px; padding: 15px 20px; box-sizing: border-box;
            display: flex; justify-content: space-between; align-items: center;
            background: rgba(255,255,255,0.02);
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }
        .user-avatar { width: 36px; height: 36px; background: var(--primary-color); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: bold; }

        .overlay {
            position: fixed; inset: 0; background: rgba(0,0,0,0.9); z-index: 3000;
            display: none; flex-direction: column; align-items: center; justify-content: center;
            backdrop-filter: blur(10px); padding: 20px;
        }

        /* 游戏板容器 - 修复过大问题 */
        #board-container { 
            position: relative; 
            margin: auto;
            box-shadow: 0 0 30px rgba(0,0,0,0.5);
            transition: transform 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            /* 关键：限制最大尺寸，防止超出屏幕 */
            max-width: 95vw;
            max-height: 70vh;
            display: flex; align-items: center; justify-content: center;
        }
        #puzzle-board { 
            display: grid; 
            background: #000; 
            border: 2px solid rgba(255,255,255,0.1);
            position: relative;
        }
        .piece { 
            box-sizing: border-box; 
            border: 0.5px solid rgba(255,255,255,0.2); 
            touch-action: none; /* 关键：禁止触摸时的页面滚动 */
            cursor: pointer;
            position: relative;
        }

        .level-card { background: var(--card-bg); border-radius: 12px; overflow: hidden; border: 1px solid rgba(255,255,255,0.1); transition: 0.3s; }
        .level-card:active { transform: scale(0.95); }
        .card-thumb { width: 100%; aspect-ratio: 1; display: flex; align-items: center; justify-content: center; background: #1a1a2e; overflow: hidden; }
        .card-thumb img { width: 100%; height: 100%; object-fit: cover; }

        .rank-item { display: flex; align-items: center; padding: 12px; margin-bottom: 8px; background: rgba(255,255,255,0.03); border-radius: 8px; width: 100%; box-sizing: border-box; }
        .rank-item.is-me { border: 1px solid var(--success-green); background: rgba(46, 204, 113, 0.1); }
        
        .btn { padding: 10px 20px; border-radius: 8px; border: none; font-weight: bold; cursor: pointer; transition: 0.2s; }
        .btn-primary { background: var(--primary-color); color: white; }
        
        #victory-img { position: absolute; inset: 0; width: 100%; height: 100%; opacity: 0; transition: 0.5s; pointer-events: none; z-index: 50; }
        .active { display: flex; flex-direction: column; align-items: center; width: 100%; }
        .screen { display: none; }
    </style>
</head>
<body>

    <div id="auth-modal" class="overlay">
        <div style="background: #1a1a2e; padding: 30px; border-radius: 16px; width: 85%; max-width: 320px; text-align: center;">
            <h3>设置你的昵称</h3>
            <input type="text" id="nickname-input" style="width:100%; padding:12px; margin:15px 0; background:#000; color:#fff; border:1px solid #333; border-radius:8px; box-sizing:border-box;">
            <button class="btn btn-primary" style="width:100%" onclick="registerUser()">进入游戏</button>
        </div>
    </div>

    <div id="rank-modal" class="overlay">
        <div style="width: 100%; max-width: 400px; background: #151525; padding: 20px; border-radius: 12px; display: flex; flex-direction: column; max-height: 80vh;">
            <h2 style="color:var(--gold); text-align: center; margin-top: 0;">🏆 世界排名</h2>
            <div id="rank-list" style="flex: 1; overflow-y:auto; margin-bottom:20px;"></div>
            <button class="btn" style="width: 100%; background: #333; color: #fff;" onclick="closeRank()">返回</button>
        </div>
    </div>

    <div id="user-bar">
        <div style="display:flex; align-items:center; gap:10px;">
            <div class="user-avatar" id="u-avatar">?</div>
            <div>
                <div id="u-name" style="font-size:0.9rem; font-weight:bold;">Loading...</div>
                <div id="u-lv" style="font-size:0.7rem; opacity:0.6;">Lv.0</div>
            </div>
        </div>
        <button class="btn" style="background:rgba(241,196,15,0.2); color:var(--gold)" onclick="openRank()">排行榜</button>
    </div>

    <div id="menu-screen" class="screen active">
        <div id="level-grid" style="display:grid; grid-template-columns:repeat(auto-fill, minmax(100px, 1fr)); gap:15px; width:90%; max-width:800px; padding:20px 0;"></div>
    </div>

    <div id="game-screen" class="screen">
        <div style="width:90%; display:flex; justify-content:space-between; align-items:center; margin:15px 0;">
            <button class="btn" onclick="showMenu()" style="background:#333; color:white;">← 退出</button>
            <span id="game-info">Level 1</span>
        </div>
        <div id="board-container">
            <div id="puzzle-board"></div>
            <img id="victory-img" src="">
        </div>
    </div>

    <script>
        // --- 1. Supabase 配置 ---
        const SB_URL = 'https://afdnvaysvwdmbnxklogt.supabase.co';
        const SB_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFmZG52YXlzdndkbWJueGtsb2d0Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njk2NzQyNzMsImV4cCI6MjA4NTI1MDI3M30.KI1Mi3cNBwy1A0-Fx9V9DIITzgExyhQgLRGvEwDWFsE';
        
        // 修复：使用 _db 而不是 supabase，避免与 CDN 全局变量冲突
        let _db = null;
        try {
            _db = supabase.createClient(SB_URL, SB_KEY);
        } catch(e) {
            console.error("Supabase init error:", e);
            alert("游戏服务连接失败，排行榜可能不可用");
        }

        // --- 2. 游戏状态 ---
        let completedLevels = JSON.parse(localStorage.getItem('puz_done_v47') || '[]');
        let currentLevel = 1, pieces = [], rows, cols, pW, pH;
        let isMoving = false;

        // --- 3. 核心逻辑：关卡与适配 ---
        function startLevel(id) {
            currentLevel = id;
            const img = new Image();
            // 假设图片在根目录下，命名为 1.jpg, 2.jpg 等
            img.src = `${id}.jpg`; 
            
            img.onload = () => {
                const ratio = img.width / img.height;
                cols = 3 + Math.floor((id-1)/5); // 难度曲线
                rows = Math.round(cols / ratio);

                // --- 修复图片过大问题 ---
                // 计算最大可用空间 (宽95%，高65%)
                const maxW = window.innerWidth * 0.95;
                const maxH = window.innerHeight * 0.65; 

                // 尝试按宽度适配
                pW = Math.floor(maxW / cols);
                pH = Math.floor((pW * cols / ratio) / rows);

                // 如果高度超标，则按高度重新计算
                if (pH * rows > maxH) {
                    pH = Math.floor(maxH / rows);
                    pW = Math.floor((pH * rows * ratio) / cols);
                }

                setupGame(img.src);
            };
            img.onerror = () => {
                alert("加载图片失败，请检查图片文件是否存在 (例如 1.jpg)");
                showMenu();
            };
        }

        function setupGame(url) {
            const board = document.getElementById('puzzle-board');
            board.innerHTML = '';
            board.style.width = `${pW * cols}px`;
            board.style.height = `${pH * rows}px`;
            
            const vImg = document.getElementById('victory-img');
            vImg.src = url;
            vImg.style.opacity = '0';
            
            // 生成拼图数据
            pieces = [];
            for(let i=0; i<rows*cols; i++) {
                pieces.push({ id: i, cur: i, bx: (i%cols)*pW, by: Math.floor(i/cols)*pH });
            }
            
            // 打乱拼图
            let pool = pieces.map(p => p.id).sort(() => Math.random() - 0.5);
            pieces.forEach((p, i) => p.cur = pool[i]);

            renderPieces(url);
            
            document.getElementById('menu-screen').classList.remove('active');
            document.getElementById('game-screen').classList.add('active');
            document.getElementById('game-info').innerText = `Level ${currentLevel} (${cols}x${rows})`;
        }

        function renderPieces(url) {
            const board = document.getElementById('puzzle-board');
            board.innerHTML = '';
            
            // 按当前位置排序渲染，保证 DOM 顺序正确
            pieces.sort((a,b) => a.cur - b.cur).forEach(p => {
                const div = document.createElement('div');
                div.className = 'piece';
                div.style.width = `${pW}px`; div.style.height = `${pH}px`;
                div.style.backgroundImage = `url(${url})`;
                div.style.backgroundPosition = `-${p.bx}px -${p.by}px`;
                div.style.backgroundSize = `${pW*cols}px ${pH*rows}px`;
                div.dataset.id = p.id; // 绑定原始 ID
                
                // 视觉融合优化
                const r = Math.floor(p.cur/cols), c = p.cur%cols;
                const right = pieces.find(x => x.cur === p.cur+1);
                const down = pieces.find(x => x.cur === p.cur+cols);
                
                // 如果右侧是正确的邻居，去除右边框
                if (c < cols-1 && right?.id === p.id+1 && Math.floor(p.id/cols) === Math.floor(right.id/cols)) {
                    div.style.borderRightColor = 'transparent';
                }
                // 如果下方是正确的邻居，去除下边框
                if (r < rows-1 && down?.id === p.id+cols) {
                    div.style.borderBottomColor = 'transparent';
                }

                div.onpointerdown = e => handleDrag(e, p.id, url);
                board.appendChild(div);
            });
        }

        // --- 4. 拖拽与融合逻辑 ---
        function handleDrag(e, id, url) {
            if(isMoving) return;
            const group = getGroup(id); // 获取融合组
            const startX = e.clientX, startY = e.clientY;
            const els = group.map(gid => document.querySelector(`[data-id="${gid}"]`));
            
            // 提升层级
            els.forEach(el => { 
                if(el) { el.style.zIndex = 1000; el.style.transition = 'none'; }
            });

            window.onpointermove = (ev) => {
                const dx = ev.clientX - startX, dy = ev.clientY - startY;
                els.forEach(el => {
                    if(el) el.style.transform = `translate(${dx}px, ${dy}px)`;
                });
            };

            window.onpointerup = (ev) => {
                window.onpointermove = null; window.onpointerup = null;
                const dx = ev.clientX - startX, dy = ev.clientY - startY;
                
                // 计算移动格子数
                const dC = Math.round(dx/pW), dR = Math.round(dy/pH);

                if (dC !== 0 || dR !== 0) {
                    let canMove = true;
                    const updates = [];
                    
                    // 检查组内每个块是否能移动到目标位置
                    for(let gid of group) {
                        const p = pieces.find(x => x.id === gid);
                        const nr = Math.floor(p.cur/cols)+dR, nc = (p.cur%cols)+dC;
                        if(nr<0 || nr>=rows || nc<0 || nc>=cols) { canMove = false; break; }
                        updates.push({p, to: nr*cols+nc, from: p.cur});
                    }

                    if(canMove) {
                        const targetPos = updates.map(u => u.to);
                        // 找到目标位置上的"受害者"（非本组的块）
                        const victims = pieces.filter(x => targetPos.includes(x.cur) && !group.includes(x.id));
                        const vacants = updates.map(u => u.from).filter(v => !targetPos.includes(v));
                        
                        // 交换位置
                        updates.forEach(u => u.p.cur = u.to);
                        victims.forEach((v, i) => v.cur = vacants[i]);
                    }
                }
                renderPieces(url); // 重新渲染
                checkWin();
            };
        }

        function getGroup(sid) {
            const group = new Set([sid]), queue = [sid];
            while(queue.length > 0) {
                const id = queue.shift(), p = pieces.find(x => x.id === id);
                const r = Math.floor(p.cur/cols), c = p.cur%cols;
                
                // 检查上下左右四个方向
                const neighbors = [
                    {dr:0, dc:1,  cond: n => n.id === p.id+1 && Math.floor(p.id/cols) === Math.floor(n.id/cols)}, // 右
                    {dr:0, dc:-1, cond: n => n.id === p.id-1 && Math.floor(p.id/cols) === Math.floor(n.id/cols)}, // 左
                    {dr:1, dc:0,  cond: n => n.id === p.id+cols}, // 下
                    {dr:-1,dc:0,  cond: n => n.id === p.id-cols}  // 上
                ];
                
                neighbors.forEach(n => {
                    const nr = r+n.dr, nc = c+n.dc;
                    if(nr>=0 && nr<rows && nc>=0 && nc<cols) {
                        const found = pieces.find(x => x.cur === nr*cols+nc);
                        // 如果邻居存在，且ID连续（匹配正确），则加入组
                        if(found && !group.has(found.id) && n.cond(found)) {
                            group.add(found.id); queue.push(found.id);
                        }
                    }
                });
            }
            return Array.from(group);
        }

        // --- 5. 胜利与数据同步 ---
        async function checkWin() {
            if (pieces.every(p => p.id === p.cur)) {
                isMoving = true;
                // 胜利特效
                document.getElementById('victory-img').style.opacity = '1';
                document.getElementById('board-container').style.transform = 'scale(1.05)';
                
                // 保存进度
                if (!completedLevels.includes(currentLevel)) {
                    completedLevels.push(currentLevel);
                    localStorage.setItem('puz_done_v47', JSON.stringify(completedLevels));
                }

                // 同步数据
                await syncMyData();
                updateUI();

                setTimeout(() => {
                    isMoving = false;
                    showMenu();
                }, 2000);
            }
        }

        async function syncMyData() {
            if(!_db) return;
            const uid = localStorage.getItem('puz_uid_v47');
            const nick = localStorage.getItem('puz_nick_v47');
            if(!uid || !nick) return;

            try {
                // Upsert 数据到 Supabase
                const { error } = await _db.from('rankings').upsert({
                    id: uid, 
                    nickname: nick, 
                    max_level: completedLevels.length,
                    updated_at: new Date()
                });
                if(error) console.error("Sync Error:", error);
            } catch(e) { console.error(e); }
        }

        // --- 6. UI 与初始化 ---
        async function openRank() {
            const modal = document.getElementById('rank-modal');
            const list = document.getElementById('rank-list');
            modal.style.display = 'flex';
            list.innerHTML = '<div style="text-align:center; padding:20px; color:#aaa;">正在加载排行榜...</div>';

            if(!_db) {
                list.innerHTML = '<div style="text-align:center; padding:20px;">排行榜服务暂不可用</div>';
                return;
            }

            try {
                // 读取前30名
                const { data, error } = await _db.from('rankings')
                    .select('*')
                    .order('max_level', {ascending: false})
                    .order('updated_at', {ascending: true}) // 等级相同时，先达成的排前面
                    .limit(30);

                if(error) throw error;

                const myUid = localStorage.getItem('puz_uid_v47');
                
                list.innerHTML = (data || []).map((r, i) => `
                    <div class="rank-item ${r.id === myUid ? 'is-me' : ''}">
                        <div style="width:30px; font-weight:bold; color:${i<3?'var(--gold)':'#fff'}">${i+1}</div>
                        <div style="flex:1; overflow:hidden; text-overflow:ellipsis; white-space:nowrap;">
                            ${escapeHtml(r.nickname)} ${r.id === myUid ? '(我)' : ''}
                        </div>
                        <div style="color:var(--primary-color); font-weight:bold;">Lv.${r.max_level}</div>
                    </div>
                `).join('') || '<div style="text-align:center; padding:20px;">暂无排名，快来抢占第一！</div>';
            } catch(e) { 
                console.error(e);
                list.innerHTML = '<div style="text-align:center; padding:20px; color:red;">加载失败，请检查网络</div>'; 
            }
        }

        function init() {
            const nick = localStorage.getItem('puz_nick_v47');
            if(!nick) {
                document.getElementById('auth-modal').style.display = 'flex';
            } else {
                updateUI();
                renderMenu();
            }
        }

        function registerUser() {
            const input = document.getElementById('nickname-input');
            const nick = input.value.trim();
            if(!nick) return alert("请输入昵称");
            
            // 生成唯一 ID 并保存
            const uid = 'ID_' + Math.random().toString(36).substr(2, 9);
            localStorage.setItem('puz_nick_v47', nick);
            localStorage.setItem('puz_uid_v47', uid);
            
            document.getElementById('auth-modal').style.display = 'none';
            syncMyData().then(() => {
                updateUI();
                renderMenu();
            });
        }

        function updateUI() {
            const nick = localStorage.getItem('puz_nick_v47');
            document.getElementById('u-name').innerText = nick || '匿名';
            document.getElementById('u-avatar').innerText = (nick || '?')[0].toUpperCase();
            document.getElementById('u-lv').innerText = `已完成: ${completedLevels.length}`;
        }

        function renderMenu() {
            const grid = document.getElementById('level-grid');
            grid.innerHTML = '';
            
            // 逻辑：加载当前完成数量 + 5 的关卡
            const showCount = completedLevels.length + 5;
            for(let i=1; i<=showCount; i++) {
                const isUnlocked = i === 1 || completedLevels.includes(i-1);
                const isDone = completedLevels.includes(i);
                
                const card = document.createElement('div');
                card.className = 'level-card';
                card.style.opacity = isUnlocked ? '1' : '0.4';
                card.innerHTML = `
                    <div class="card-thumb">${isDone ? `<img src="${i}.jpg">` : `<span>${i}</span>`}</div>
                    <div style="padding:8px; text-align:center; font-size:0.8rem;">
                        ${isDone ? '✅ 已完成' : (isUnlocked ? '🔥 挑战' : '🔒 未解锁')}
                    </div>
                `;
                if(isUnlocked) card.onclick = () => startLevel(i);
                grid.appendChild(card);
            }
        }

        function showMenu() {
            document.getElementById('game-screen').classList.remove('active');
            document.getElementById('menu-screen').classList.add('active');
            document.getElementById('board-container').style.transform = 'none';
            renderMenu();
        }

        function closeRank() { document.getElementById('rank-modal').style.display = 'none'; }
        function escapeHtml(text) { return text.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;"); }

        window.onload = init;
    </script>
</body>
</html>